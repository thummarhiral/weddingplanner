<?php

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * @return void
     */
    public function run()
    {
    	// DB::Table('users')->insert([
    	// 	'Name'->str_random(10),
    	// 	'email'->str_random(10),
    	// 	'password'->bcryt('secret'),

    	// ]);
        // $this->call(UsersTableSeeder::class);
    }
}
